<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Student Form</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="form-container">
        <div class="form-header">
            <h1>Add New Student</h1>
            <p>Fill in the student information below</p>
        </div>

        <div id="successMessage" class="success" style="display: none;">
            Student added successfully!
        </div>
        
        <form id="newStudentForm" method="POST" action="/new">
            <div class="form-row">
                <div class="form-group">
                    <label for="firstName" class="form-label">First Name *</label>
                    <input type="text" id="firstName" name="first_name" class="form-input" required>
                     <%- include("partials/errorMessage", {path: "first_name"}) %>
                    
                </div>
                
                <div class="form-group">
                    <label for="lastName" class="form-label">Last Name *</label>
                    <input type="text" id="lastName" name="last_name" class="form-input" required>
                    <%- include("partials/errorMessage", {path: "last_name"}) %>
                </div>
            </div>

            <div class="form-group">
                <label for="gradeLevel" class="form-label">Grade Level *</label>
                <select id="gradeLevel" name="grade_level" class="form-select" required>
                    <option value="">Select Grade Level</option>
                    <option value="11">Grade 11</option>
                    <option value="12">Grade 12</option>
                </select>
                <%- include("partials/errorMessage", {path: "grade_level"}) %>
            </div>

            <div class="form-group">
                <label for="gradeAverage" class="form-label">Grade Average *</label>
                <input type="number" id="gradeAverage" name="grade_average" class="form-input" 
                       min="0" step="0.01" placeholder="Enter grade average (0-100)" required>
                <%- include("partials/errorMessage", {path: "grade_average"}) %>
            </div>

            <div class="form-group">
                <label for="strand" class="form-label">Strand *</label>
                <select id="strand" name="strand" class="form-select" required>
                    <option value="">Select Strand</option>
                    <option value="STEM">STEM - Science, Technology, Engineering and Mathematics</option>
                    <option value="ABM">ABM - Accountancy, Business and Management</option>
                    <option value="HUMSS">HUMSS - Humanities and Social Sciences</option>
                    <option value="GAS">GAS - General Academic Strand</option>
                    <option value="TVL">TVL - Technical-Vocational-Livelihood</option>
                </select>
                <%- include("partials/errorMessage", {path: "strand"}) %>
            </div>

            <div class="form-buttons">
                <button type="submit" class="btn btn-primary">Add Student</button>
            </div>
        </form>
    </div>

    <script>
        
    </script>
</body>
</html>